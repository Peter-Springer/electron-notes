const {shell, remote} = require('electron');
// const Vue = require('vue')
// const Main = require('./main.js');
const application = document.querySelector('.app')

// new Vue({ // eslint-disable-line no-new
//   el: '.app',
//   render: (h) => h(Main)
// })

application.innerHTML = 'hellooo'
